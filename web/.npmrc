# `engines.node` in package.json is a hard requirement, not a suggestion: ESLint 10
# won't run below Node 20.19, and web/dist is committed + byte-compared by CI, so it
# must be built on the toolchain in .nvmrc.
engine-strict=true
